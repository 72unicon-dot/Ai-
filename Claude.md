# Claude Harness 기준 문서 (웹개발 10일 프로젝트)

## 1) 프로젝트 개요
- **목표**: 이미지 기반 웹개발 프로세스를 따르되, 10일 내 MVP를 완성한다.
- **기간**: 총 10일
- **구성**: 프론트엔드 + 백엔드 + DB
- **개발 원칙**
  - 매일 작업 종료 후 GitHub에 커밋/업로드
  - AI가 매일 산출물을 스스로 검증(코드/테스트/API 동작)
  - API는 **무료(Free Tier / Open API)만 사용**
  - 매일 변경 요구사항을 리뷰하고 본 문서(Claude.md)에 반영

---

## 2) 역할 및 책임 (AI Agent 기준)
### Frontend Agent
- UI 구현, 상태관리, API 연동, 접근성/반응형 점검
- 일일 검증:
  - 빌드 성공
  - 주요 페이지 렌더링 확인
  - API 연동 시 에러 핸들링 확인

### Backend Agent
- API 설계/구현, 인증/권한, 비즈니스 로직, 로깅
- 일일 검증:
  - 서버 기동 확인
  - 핵심 엔드포인트 정상/예외 응답 확인
  - 무료 API 연동 제한/쿼터 상태 확인

### DB Agent
- 스키마 설계, 마이그레이션, 인덱스/성능, 데이터 무결성
- 일일 검증:
  - 마이그레이션 적용/롤백 가능 여부
  - 참조 무결성, 중복/NULL 제약 확인
  - 샘플 데이터 기반 조회 성능 점검

---

## 3) 10일 수행 계획 (기본안)
> 일일 요구사항 변경에 따라 조정 가능

- **Day 1**: 요구사항 확정, 아키텍처/기술스택 결정, 저장소 초기화
- **Day 2**: 화면 구조(와이어 수준), API 명세 초안, DB ERD 초안
- **Day 3**: FE 기본 라우팅/컴포넌트, BE 기본 서버/헬스체크, DB 1차 스키마
- **Day 4**: 핵심 기능 1차 구현 (CRUD 중심)
- **Day 5**: 핵심 기능 2차 구현 + FE/BE 통합
- **Day 6**: 인증/권한(필요 시), 예외 처리, 입력 검증 보강
- **Day 7**: 성능/쿼리/캐시(필요 시) 및 UX 개선
- **Day 8**: 테스트 보강(단위/통합/E2E 선택), 버그 수정
- **Day 9**: 배포 리허설, 운영 문서화, 회귀 점검
- **Day 10**: 최종 검증, 릴리스, 회고 및 다음 액션 정의

---

## 4) 일일 작업 하네스 (Daily Harness)
아래 순서를 **매일 작업 종료 전** 실행한다.

1. **요구사항 변경 리뷰**
   - 당일 변경 요청/이슈를 수집
   - 우선순위(P0/P1/P2) 재정렬
   - 영향도(Frontend/Backend/DB) 기록

2. **구현 결과 점검**
   - FE: 빌드/화면/API 연결 확인
   - BE: 서버 기동/핵심 API 동작 확인
   - DB: 마이그레이션/쿼리 무결성 확인

3. **AI 자체 검증(Self-Validation)**
   - 기능 체크리스트 기반 Pass/Fail 기록
   - 실패 항목은 원인과 재현 방법 포함
   - 다음날 우선 해결 항목 등록

4. **GitHub 업로드**
   - 브랜치 정리 → 커밋 → 푸시
   - 커밋 메시지에 범위 명시:
     - `feat(frontend): ...`
     - `feat(backend): ...`
     - `feat(db): ...`
     - `fix: ...`, `chore: ...`, `docs: ...`

5. **Claude.md 업데이트**
   - 당일 변경 요구사항 반영
   - 일정(10일 계획) 재조정 여부 기록
   - 리스크 및 대응전략 업데이트

---

## 5) 무료 API 사용 기준
- 유료 플랜/API Key 과금 기능은 사용 금지
- 사용 전 체크:
  - 무료 호출량/쿼터 제한
  - 인증 방식 및 보안 이슈
  - 상업적 사용 가능 여부(라이선스)
- 장애 대비:
  - 대체 무료 API 1개 이상 확보
  - 실패 시 graceful fallback 처리

---

## 6) 품질 게이트 (Definition of Done, 기본)
다음 조건 충족 시 당일 작업 완료로 간주한다.

- [ ] 요구사항 변경사항이 Claude.md에 반영됨
- [ ] FE/BE/DB 변경분이 최소 1회 이상 검증됨
- [ ] 핵심 기능 테스트 결과가 기록됨 (Pass/Fail)
- [ ] 오류/리스크/미해결 이슈가 명시됨
- [ ] GitHub에 당일 작업 커밋/푸시 완료

---

## 7) 일일 기록 템플릿 (복사해서 사용)
```md
## YYYY-MM-DD Daily Log

### 1. 오늘의 변경 요구사항
- (요구사항)

### 2. 구현 내용
- Frontend:
- Backend:
- DB:

### 3. AI 자체 검증 결과
- FE 체크: Pass/Fail
- BE 체크: Pass/Fail
- DB 체크: Pass/Fail
- 통합 체크: Pass/Fail
- 비고(실패 원인/재현 방법):

### 4. 무료 API 점검
- 사용 API:
- 호출량/제한:
- 장애 대응:

### 5. GitHub 업로드
- Branch:
- Commit:
- PR/링크:

### 6. 리스크 및 다음 작업
- 리스크:
- 내일 우선순위:
```

---

## 8) 운영 규칙
- 문서 우선순위: **최신 Claude.md > 이전 계획**
- 요구사항 변경 시 구두 합의가 아닌 문서 반영을 기준으로 한다.
- 범위를 벗어나는 요청은 `Backlog`로 분리하고 일정 영향도를 먼저 평가한다.

